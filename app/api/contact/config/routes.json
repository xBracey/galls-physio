{
  "routes": [
    {
      "method": "GET",
      "path": "/contact",
      "handler": "contact.find"
    },
    {
      "method": "PUT",
      "path": "/contact",
      "handler": "contact.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/contact",
      "handler": "contact.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
